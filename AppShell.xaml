<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="StudyBuddyMobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:StudyBuddyMobile"
    xmlns:views="clr-namespace:StudyBuddyMobile.Views"
    Title="StudyBuddyMobile">

    <!-- Define Colors -->
    <Shell.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#4f46e5</Color>
            <Color x:Key="PrimaryDark">#3730a3</Color>
            <Color x:Key="Secondary">#10b981</Color>
        </ResourceDictionary>
    </Shell.Resources>

    <!-- Auth Shell (No Navigation Bar) -->
    <Shell.FlyoutBehavior>
        Disabled
    </Shell.FlyoutBehavior>

    <!-- Login/Register Pages (shown when not authenticated) -->
    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="LoginPage" />

    <ShellContent
        Title="Register"
        ContentTemplate="{DataTemplate views:RegisterPage}"
        Route="RegisterPage" />

    <!-- Main App (shown when authenticated) -->
    <TabBar Route="MainTabs">
        <ShellContent
            Title="Dashboard"
            ContentTemplate="{DataTemplate views:DashboardPage}"
            Route="DashboardPage" />

        <ShellContent
            Title="Resources"
            ContentTemplate="{DataTemplate views:StudyResourcesPage}"
            Route="StudyResourcesPage" />

        <ShellContent
            Title="Groups"
            ContentTemplate="{DataTemplate views:StudyGroupsPage}"
            Route="StudyGroupsPage" />

        <ShellContent
            Title="Profile"
            ContentTemplate="{DataTemplate views:ProfilePage}"
            Route="ProfilePage" />
    </TabBar>

</Shell>